function documents_model.m2m_to_obj(parent_uuid, conector_model_name, child_model )
    -- reuturns all the models that belog to a model
    -- using a many to many relation that uses a conector model


end